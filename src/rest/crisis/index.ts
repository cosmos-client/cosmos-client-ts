import { cosmos } from '../../proto';
import { codec } from '../../types';

// export * as crisis from './module';

codec.register('/cosmos.crisis.v1beta1.MsgVerifyInvariant', cosmos.crisis.v1beta1.MsgVerifyInvariant);
